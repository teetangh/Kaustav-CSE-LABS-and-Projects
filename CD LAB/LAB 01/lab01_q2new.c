#include <stdio.h>

int main(int argc, char const *argv[])
{
    FILE *fa, *fb;

    fa = fopen("input.txt", "r");
    fb = fopen("output.txt", "w+");

    
    while ()

        fclose(fa);
    fclose(fb);

    return 0;
}
