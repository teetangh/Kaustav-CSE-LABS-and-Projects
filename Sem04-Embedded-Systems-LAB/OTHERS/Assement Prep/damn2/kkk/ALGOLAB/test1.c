#include <stdio.h>
#include <stdlib.h>

int costMatrix[10][10];
int n;
int done[10] int min = 999;
int least()
{
	for (int i = 0; i < n; i++)
}
int main(void)
{
	printf("Enter number of cities: ");
	scanf("%d", &n);
	printf("Enter the cost adjacency matrix\n") for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < n; j++)
			scanf("%d", &costMatrix[i][j]);
		done[i] = 1;
	}
}
